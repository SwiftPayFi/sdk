name: sdk/x402-fastapi-guard · ci

on:
  pull_request:
    paths:
      - 'x402-fastapi-guard/**'
      - '.github/workflows/x402-fastapi-guard-ci.yml'
  push:
    branches: [main]
    paths:
      - 'x402-fastapi-guard/**'
      - '.github/workflows/x402-fastapi-guard-ci.yml'

concurrency:
  group: sdk-x402-fastapi-guard-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: ruff · mypy · pytest · build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
    defaults:
      run:
        working-directory: x402-fastapi-guard
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Create venv & install
        run: |
          uv venv --python ${{ matrix.python-version }}
          uv pip install -e ../python
          uv pip install -e ".[dev]"

      - name: Ruff lint
        run: .venv/bin/ruff check .

      - name: Ruff format check
        run: .venv/bin/ruff format --check .

      - name: Mypy
        run: .venv/bin/mypy src

      - name: Pytest with coverage
        run: .venv/bin/pytest --cov --cov-report=term

      - name: Build (sdist + wheel)
        if: matrix.python-version == '3.12'
        run: .venv/bin/python -m build

      - name: Upload dist
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: x402-fastapi-guard/dist/
          if-no-files-found: error
